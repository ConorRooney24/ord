# ord
Data structure implementations in C.

# installation
- Clone the repo using ```git clone git@github.com:ConorRooney24/ord.git```.
- Then cd into the new directory with ```cd ord```.
- Install with ```sudo make install```.

# todo
- Add functions for working with vectors
